alias tmux='tmux -2'

alias lls="ls -l `find . -maxdepth 1 -type l -print`"
alias ll="ls -ltr"
alias fba="cd ~/fbsource/fbandroid"

if [ -f ${HOME}/.aliases_local ]; then
	source ${HOME}/.aliases_local
fi
